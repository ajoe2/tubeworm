[project]
name = "tubeworm"
version = "0.1.0"
description = "A localhost YouTube downloader and clip trimmer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.138.1",
    "sse-starlette>=3.4.5",
    "uvicorn[standard]>=0.49.0",
    "yt-dlp[default]>=2026.6.9",
]
